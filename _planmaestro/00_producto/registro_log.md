# Registro de trabajo pendiente

Fuente única de verdad sobre qué falta. Ordenado por prioridad dentro de cada épica.

**Cuando aparezca una idea fuera del alcance de la iteración activa, se añade aquí
—en «Sin asignar» o en la épica que corresponda— en vez de implementarla.**

**Estados:** ⚪ Pendiente · 🔵 En curso · 🟢 Hecha · ⏸️ Aplazada · ❌ Descartada

---

## Épica 10 · Plataforma Cloudflare

| Estado | Iteración | Tarea                                                                        |
|---|---|------------------------------------------------------------------------------|
| 🟢 | 11 | Desconectar GitHub Pages, que era el despliegue anterior                     |
| 🟢 | 11 | Publicar el sitio en Cloudflare Pages sin regresiones                        |
| 🟢 | 11 | Decidir si el despliegue compila o publica el repositorio tal cual (ADR-010) |
| 🟢 | 12 | Crear la base D1 y la capa de datos                                          |
| 🟢 | 12 | Decidir el dominio del Worker (ADR-011)                                      |
| 🟢 | 12 | Definir el formato de error del Worker, base del respaldo de ADR-008         |
| 🟢 | 12 | Desarrollo local contra una base D1 local                                    |
| 🟢 | 12 | Verificar la capa de datos en la dirección pública, tras el primer despliegue con `wrangler.toml` |
| 🟢 | 13 | El manual documenta la creación de la base **por el panel** (su paso 1), pero la base de pruebas se creó por línea de comandos con `wrangler d1 create`, ruta que el manual no menciona. Decidir cuál es la ruta oficial y reescribir el paso 1 en consecuencia; si conviven las dos, decir cuándo se usa cada una |
| 🟢 | 13 | El manual `90-manual/capa-de-datos-y-base-d1.md` está escrito para una base única: su paso 1 fija `examen-td-js-produccion` y todos sus comandos nombran esa base. Con dos entornos tiene que cubrir ambas y dejar claro cuál se está tocando en cada comando. **No se corrige hasta que el ensayo del manual traiga los huecos reales** |
| 🟢 | 13 | Declarar `[env.preview]` con base propia. Verificado en un despliegue de vista previa real: leyó la base de pruebas |
| 🟢 | 13 | Separar entorno de pruebas y producción. Demostrado consultando ambas bases en el mismo instante durante el ensayo de restauración |
| 🟢 | 13 | Gestión de credenciales fuera del repositorio                                |
| 🟢 | 13 | Procedimiento de respaldo en `90-manual/respaldo-y-restauracion.md`, con la restauración ensayada contra pruebas destruyendo un dato real |

| 🟢 | 13 | **H-011** · `npm run verificar` dependía del terminal y fallaba a mitad de camino. Reescrito como `scripts/verificar.mjs`, con tres veredictos y sin depender de git para la comprobación central |
| 🟢 | 13 | Que el manual permita publicar desde un clon sin preguntarle nada al autor. Cerrado al resolver H-011: los manuales explican los tres veredictos y ya no dependen de qué terminal use quien los siga |

## Épica 20 · Persistencia de preguntas

| Estado | Iteración | Tarea |
|---|---|---|
| 🟢 | 21 | **El sesgo de la respuesta correcta se corrige barajando, no reescribiendo preguntas** (decisión del autor, 2026-09-04). Consecuencia para el esquema: la correcta no puede identificarse por posición, salvo en la única pregunta con `fijo = true`, que no se baraja. Cierra casi por completo la decisión 2 de esta iteración |
| 🟢 | 21 | **El banco nuevo suma, no reemplaza: 405 preguntas.** Publicado como **ADR-016**, con los dos formatos tabulados y las consecuencias de unificarlos |
| ⚪ | 21 | **Ubicacion definitiva del banco.** Hoy los siete `modulo-0N.json` estan en `_planmaestro/00_producto/cuestionarios/`, que es temporal. Al proponer la definitiva hay que **comprobar de forma explicita que esa carpeta no termine copiada a `dist/`**: un archivo con las respuestas correctas servido al navegador deja el simulacro sin sentido. Hoy es seguro porque `LISTA_COPIA` no nombra `_planmaestro/`, pero eso deja de ser cierto en cuanto el banco se mueva |
| 🟢 | 21 | **El informe de la IA que genero los cuestionarios no sirve como fuente unica.** Resuelto con `scripts/informe-banco.mjs`, que revisa los ocho archivos desde cero empezando por si cada uno se puede leer. `modulo-02.json` no era JSON valido —faltaba la comilla de apertura del enunciado de la pregunta 3— y el informe no lo detecto. El script de verificacion tiene que revisar todo de nuevo desde cero, empezando por lo mas basico: que cada archivo se pueda leer |
| ⚪ | 21 | Ensayar el manual entero, desde su paso 1, al crear la base del banco de preguntas. **Cierra el criterio 7 de la iteración 12, aplazado explícitamente hasta acá**: el procedimiento describe pasos que sí ocurrieron, pero nunca se ha seguido de principio a fin sobre una base que todavía no existe. La iteración 21 crea una, así que el ensayo ocurre solo |
| 🟢 | 21 | Borrar `prueba_tuberia` de **las dos bases de la nube**, produccion y pruebas. La crearon las iteraciones 12 y 13 para probar la tubería y sigue ahí: la primera migración del esquema real tiene que retirarla de ambas. **Cerrada el 2026-09-09 en la iteracion 24.** En pruebas ya no estaba —la retiró el vaciado del ensayo remoto del 2026-09-05—. En producción la borró el autor con un `DROP TABLE IF EXISTS` escrito a mano, después de exportar el respaldo y después de cotejar el uuid `cff1686b-3b24-4892-9a10-4306684e0127`. Antes del `DROP` producción listaba `_cf_KV` y `prueba_tuberia`; después, `_cf_KV`, `alternativa`, `migracion`, `modulo` y `pregunta`. **Su única copia hoy es `d1/respaldo-banco.sql`**, y ese respaldo previo es exactamente lo que hacía seguro el borrado |
| 🟢 | 21 | Diseñar el esquema del banco en D1. Migración en `d1/migraciones/001-banco-de-preguntas.sql`, documentado campo por campo en `90-manual/esquema-del-banco.md` |
| 🟢 | 21 | Decidir el modelado de las alternativas. **ADR-018**: tabla aparte |
| 🟢 | 21 | Decidir el tratamiento de las preguntas retiradas. **ADR-020**: se marcan y se ocultan tras una vista, nunca se borran |
| 🟢 | 21 | Migraciones versionadas y repetibles. Comprobado corriendo la migración dos veces contra D1 local: 10 sentencias, sin error ni duplicados |
| 🟢 | 21 | Decidir cómo se identifica la respuesta correcta sin depender de la posición. **ADR-019**: bandera `es_correcta` en la alternativa, con la unicidad forzada por un índice único parcial |
| 🟢 | 21 | Decidir dónde viven el título y el ícono de cada módulo (decisión añadida por el autor). **ADR-021**: tabla `modulo` con llave foránea desde `pregunta` |
| 🟢 | 21 | Consulta de verificación de lo que el esquema no puede exigir: `d1/verificar-banco.sql` con `scripts/verificar-banco.mjs`. **Cerrada el 2026-09-05:** el autor provocó el tercer veredicto contra la nube en sus dos formas —nombre inexistente en la cuenta, y base de pruebas vaciada, donde el error lo devolvió D1— y repobló dejando `BANCO VERIFICADO` código 0. Los criterios que dependían de ella quedan cumplidos. Texto original de la anotación: **La consulta y el envoltorio están listos y probados; los criterios que dependen de ellos no se cierran todavía.** El envoltorio venía dando un veredicto que no significaba nada (**H-013**) y lo que se probó en su momento se probó con él roto. Se cierran cuando el autor repita el paso 4 de la iteración 21 sobre una base local recién reconstruida |
| 🟢 | 21 | **H-013** · `verificar-banco` anunciaba `BANCO CON PROBLEMAS / undefined -> undefined` pasara lo que pasara: no distinguía «el banco falla» de «no pude preguntarle nada al banco». Reescrito con tres veredictos —`BANCO VERIFICADO` (0), `BANCO CON PROBLEMAS` (1), `NO SE PUDO VERIFICAR` (2)—, comprobando la forma de la respuesta fila por fila y mostrando lo que dijo wrangler por las dos salidas. Sin `shell` y sin `npx`. Los tres estados provocados de verdad, en PowerShell y en Git Bash, desde un clon limpio en Windows |
| 🟢 | 21 | **Ensayo remoto completo sobre la base de pruebas: los tres veredictos de `verificar-banco` cerrados.** El 2026-09-05 el autor provocó el tercero en sus dos formas —forma A, nombre inexistente en la cuenta, con el 404 de la API; forma B, la base vaciada, con `no such table: modulo` devuelto por D1— y repobló dejando `BANCO VERIFICADO` código 0. La forma B es la que vale: es el camino remoto completo, y sobre una base vacía respondió «no pude verificar» en vez del cómodo y falso «verificado» |
| ⏸️ | 21 | **H-016** · Wrangler se cae al terminar en Windows —`Assertion failed` en `src\win\async.c`— y a veces devuelve `3221226505`. Salió en dos de las tres corridas del ensayo remoto. **No es un problema del proyecto** y va a seguir apareciendo. Se convive con él; queda escrito para que nadie lo confunda con un fallo del banco o de los guiones. Corolario que sí es del proyecto: **el código de salida de wrangler no es un testigo fiable en Windows**, y por eso `verificar-banco` interpreta lo que dijo y comprueba la forma de la respuesta antes de creérsela |
| ⚪ | 21 | **Cargar las 368 preguntas.** El esquema está listo y probado con diez filas; la carga completa desde los siete JSON y el `.js` es trabajo de la iteración 24 |
| ⚪ | 22 | **`/api/prueba` quedó apuntando a una tabla que ya no existe en pruebas.** `functions/api/prueba.js` consulta `prueba_tuberia`, y el vaciado del ensayo remoto la retiró de la base de pruebas: contra el entorno de vista previa ese extremo ya falla. **Corrección del 2026-09-05, el mismo día:** se dijo aquí que no lo llamaba ningún componente, y **era falso**. `static/js/components/estado-datos.js` importa `leerPrueba()` y la usa para dibujar el indicador del pie de `cuestionario.html`: contra vista previa ese indicador ya decía «conectado, sin poder leer». La comprobación original buscó la ruta `/api/prueba` en el código del sitio y no el nombre de la función que la envuelve, que es exactamente el error de método que este proyecto lleva tres hallazgos persiguiendo. Retirado el extremo, la función del cliente y la tabla, y el indicador migrado a `leerPreguntas()`, que es lo que de verdad quería contar |
| ⚪ | 22 | **La instantánea incluye las respuestas correctas (ADR-022).** Decisión del autor, 2026-09-05. Consecuencia que hay que implementar sin adornos: el generador no filtra ni ofusca nada. Consecuencia que hay que **escribir** donde se vea: el simulacro es una herramienta de estudio, no un instrumento de evaluación con validez |
| 🔵 | 22 | **La instantánea se genera desde la nube y sólo la genera el autor (ADR-023).** Claude Code escribe el generador y lo prueba contra D1 local; el archivo versionado lo produce el autor. **El criterio «coincide con lo que hay en D1» sólo puede cerrarlo él.** El generador debe dejar escrito dentro del archivo contra qué base corrió y cuándo |
| 🟢 | 22 | **La instantánea versionada traía una pregunta que no era del banco, y bloqueó la publicación un día.** `static/js/data/instantanea-banco.js`, commiteado en `b938c4b`, contiene **9 preguntas** cuando el banco de juguete versionado tiene **8**. La novena es la `id 11`, «Prueba de carga transaccional CLI (Editado).», con justificación «No hace falta justificar prueba.» y alternativas «Incorrecto editado!!» y «Me parece que incorrecto»: la insertó la herramienta de Antigravity en la D1 local mientras se trabajaba la rama `antigravity`, y la instantánea se regeneró el 2026-09-06 desde esa base ya contaminada. Descubierto el 2026-09-08 en el recorrido en frío. **Consecuencia si se publica así:** como producción está vacía, el sitio sirve la instantánea, y esa pregunta inventada se le muestra a un estudiante como material de estudio. **Comprobado:** una D1 local reconstruida desde `001-banco-de-preguntas.sql` + `ejemplo-banco.sql` devuelve 8 preguntas, ids 1-8, sin esa fila. **Lección de método:** la separación de la rama se auditó por autoría de archivos y eso no alcanza cuando el contenido ajeno viaja *dentro* de un archivo propio generado desde un estado compartido. **Resuelto el 2026-09-08:** D1 local borrada y reconstruida desde lo versionado, e instantánea regenerada con `npm run datos:instantanea` — 8 preguntas, ids 1-8, sello del 2026-09-08. Comprobado provocando, no razonando: la `id 11`, sus alternativas `41-44` y el texto «Prueba de carga transaccional» dan cero coincidencias en la base y en el archivo, y las 8 preguntas, sus justificaciones y sus 32 alternativas se cotejaron una a una contra `d1/ejemplo-banco.sql`. **Huella completa de la herramienta de Antigravity en la base local, medida antes de borrarla:** la pregunta `11`, las alternativas `41-44` y la columna `fecha_modificacion` de su migración 002 — que **aplicó sin registrarla en la tabla `migracion`**, donde solo constaba `001`. Ninguna de las preguntas ni alternativas existentes fue modificada: comparadas campo a campo antes y después de reconstruir, idénticas |
| 🟢 | 24 | **Los manuales enseñaban `npx wrangler` mientras la regla del proyecto lo prohíbe.** Detectado el 2026-09-08 al escribir los comandos de la nube para el autor, corregido el 2026-09-09 **antes de publicar**, y a propósito: la iteración 25 va a seguir `respaldo-y-restauracion.md` para exportar el respaldo real de producción, y ése era el peor momento para descubrir que el manual enseña un comando prohibido. No eran dos archivos sino **cinco**, más dos comandos que imprime `scripts/probar-escapado.mjs` cuando pide limpiar la base a mano: `respaldo-y-restauracion.md` (11), `esquema-del-banco.md` (16), `capa-de-datos-y-base-d1.md` (6), `administrar-el-banco.md` (3) y `escapado-del-banco.md` (1). Se cambió el comando y lo mínimo alrededor, sin reescribir el texto. **Lo que NO se tocó:** las menciones a `npx` que hablan *de* npx —la reconstrucción de H-013 en la auditoría, el motivo por el que se descartó el código de la rama `antigravity`—, porque ahí la palabra es el sujeto y no una instrucción. Una frase de `esquema-del-banco.md` quedó mal por el reemplazo mecánico —«Los `npx wrangler` directos no la tienen», que era prosa— y se reescribió como «Los comandos de wrangler directos no la tienen» |
| 🟢 | 24 | **H-022** · **Los dos extremos se contradijeron sobre el mismo hecho, y el que mentía era el de diagnóstico.** El 2026-09-09, con el esquema recién aplicado en producción y el banco en cero, sobre el sitio publicado y en el mismo minuto: `/api/preguntas` respondía `meta.vacio: TRUE` y `/api/estado` respondía `meta.vacio: FALSE`. **Comprobado en las dos direcciones** —el alias `2934ad82.examen-certificacion-td-js.pages.dev` y la canónica `examen-certificacion-td-js.pages.dev`—, con resultado idéntico; se repitió en la canónica a propósito, porque un alias puede quedar apuntando a un despliegue viejo y una evidencia tomada sólo del alias no prueba qué está sirviendo el sitio. **Causa:** `respuestaOk()` calculaba `vacio` con `Array.isArray(datos) ? … : datos == null`, y `/api/estado` entrega un **objeto**, así que daba `false` siempre. El campo no medía nada y se leía como una medición. **Es peor que el punto ciego que ya estaba anotado:** el punto ciego hacía que el diagnóstico *callara* un problema; esto hacía que lo *negara*, y en el único extremo al que alguien acude justamente cuando sospecha que algo anda mal. **Corregido:** `meta.vacio` sólo aparece cuando `datos` es una lista, y `/api/estado` cuenta `pregunta_activa` y responde `preguntas_activas` y `banco_vacio`. Provocados los tres estados; los dos extremos coinciden en los tres y `banco_vacio` cambia de valor. **Lección de método:** un campo calculado sobre la **forma** del dato y no sobre su **contenido** puede quedar clavado en un valor sin que nada avise; éste llevaba desde la iteración 12 diciendo `false`, y nadie lo miró porque hasta que hubo un banco vacío de verdad `false` era la respuesta correcta por casualidad. Detalle en la auditoría técnica |
| 🟢 | 24 | **El punto ciego de `/api/estado`, cerrado.** Hacía `SELECT 1 AS vivo` y concluía `consulta_d1: "correcta"`, consulta que **no puede fallar por el motivo que importa**: pasa igual con la base sin esquema. Ahora pregunta dos cosas —`SELECT 1` para saber si D1 contesta, y `SELECT COUNT(*) FROM pregunta_activa` para saber si el esquema está y cuánto hay— y responde `503 SIN_ESQUEMA`, código nuevo, cuando falta el esquema. **La consulta trivial se conservó a propósito**: es la única que distingue «la base no contesta» de «la base contesta pero le falta el esquema», y reemplazarla habría ganado el fallo perdiendo la distinción. Provocado borrando el estado de la D1 local: pasó de `200 · correcta` a `503 · SIN_ESQUEMA` |
| 🟢 | 24 | **El contador de la portada ya no puede mentir.** Estaba escrito a mano en `cuestionario.html` —«105 preguntas · 7 módulos»— y el 2026-09-08 la pantalla decía «105 preguntas» arriba y «copia guardada en el sitio (8 preguntas)» en el pie **al mismo tiempo**. Ahora lo escribe el componente con lo que de verdad dibujó, y **los dos números** salen del dato, no sólo el de preguntas. Provocado en cuatro estados corriendo el componente real contra el extremo real: 8 preguntas/7 módulos → se retiran las del módulo 8 → «7 preguntas · 6 módulos» → con cero activas **desaparece**. Del `<meta description>` se quitó el número en vez de hacerlo dinámico: es HTML estático servido antes de que corra nada, y un número puesto al construir sería una segunda fuente que vuelve a poder desfasarse. **Ningún número es mejor que un número falso** |
| 🟢 | 25 | **H-024** · **El texto de wrangler podía vetar a la base, y la base era la única que sabía.** La primera carga real —las 52 del módulo 2 en producción, 2026-09-09— terminó en `SIN VEREDICTO` **habiendo entrado entera**: en la misma salida wrangler decía `884 rows written` y `"success": true`. **Causa inmediata:** wrangler tiene dos caminos de escritura y `administrar-banco.mjs` conocía uno; el de importación dice «queries» y nunca «commands executed successfully». Nunca había salido porque la herramienta se verificó en la iteración 23 con lotes de 3 a 10 preguntas, **todos por el camino de archivo chico: la rama de importación no se había corrido ni una vez**. **Causa de fondo:** el análisis del texto estaba puesto como **portón** de la consulta a la base, así que un texto no reconocido cortaba antes de preguntarle a la única fuente que podía responder — la cabecera del archivo decía que el veredicto salía de dos fuentes y la implementación dejaba a la fuerte fuera de alcance. **Corregido:** el patrón reconoce los dos caminos y **la base se consulta siempre**; el texto pasó de condición a señal. Provocados los tres casos contra la D1 local, incluido romper el patrón a propósito para ver que la base manda igual. **Lección:** poner la fuente débil como condición de la fuerte convierte «no supe leer» en «no se puede saber», y la segunda es falsa |
| 🟢 | 25 | **H-023** · **PATRON: tres comprobaciones que nunca habían dicho «no» resultaron no poder decirlo.** El `meta.vacio` clavado en `false` desde la iteración 12 (H-022); las nueve restricciones del esquema, creídas por leerlas hasta que la iteración 23 les hizo rechazar algo; y el filtro de retiradas del comprobador de conversión, que informaba «ninguna colada» habiendo mirado **1 de 3** porque `retiradas.json` escribe `2` en el banco nuevo y `"Módulo 2"` en el viejo —y las del viejo ni siquiera traen `numero`—. **Lo que comparten:** ninguno fallaba, y en los tres el «sí» no era el resultado de comprobar sino **el único resultado posible**. Una comprobación que siempre pasa y una que no existe se ven iguales desde afuera, y la que siempre pasa es peor porque además tranquiliza. **Regla adoptada el 2026-09-09: toda comprobación nueva hay que hacerla fallar antes de creerle** — no probarla: verla decir «no», provocado. Si no se puede provocar que diga «no», no es una comprobación sino una afirmación con su forma. Detalle en la auditoría |
| 🟢 | 25 | **Módulo 3 cargado, comprobado y publicado.** 61 preguntas —50 `json_2026` + 11 `js_2026`— el 2026-09-10. La carga entró entera, **52 → 113**; la comprobación calza contra los dos archivos de origen, las **cuatro** retiradas del módulo no se colaron, `dificultad IS NULL` en las 61 y ninguna activa sin justificación; el paso único publicó respaldo e instantánea con sello **`"entorno": "nube"`** y **113 preguntas cada uno**. **Era la primera prueba del procedimiento de once pasos**, escrito antes de convertir a propósito, y el criterio estaba fijado de antemano: si sale sin sorpresas, sirve. Salió con dos sorpresas menores —H-019 disparado por una sesión fría y H-031, el CSS— y **ninguna del tipo que obliga a rehacer nada**. Los cinco módulos que quedan van con dos pasos más: el 0 y la tercera fila del 10 |
| 🟢 | 25 | **H-031** · **El texto del banco compila CSS, y nadie lo sabía.** Tras cargar el módulo 3, `npm run verificar` dio `DESFASADO` sobre `static/css/style.css` sin que nadie tocara estilos. **No son finales de línea: son 80 caracteres de contenido**, una regla `.collapse{visibility:collapse}` que antes no estaba. **Causa:** `tailwind.config.cjs` escanea `./static/js/**/*.js`, y ahí vive `instantanea-banco.js`, que **es el banco**; Tailwind no distingue código de contenido, así que leyó la palabra `collapse` dentro de una justificación del módulo 2 sobre jQuery —«`.collapse()` no es de jQuery sino de Bootstrap»— y emitió su clase. El desfase **entró el 2026-09-09** con el commit de la instantánea y **se hizo visible el 10**, en la primera recompilación posterior. **No es un defecto y no hay nada que arreglar en el archivo:** recompilar produce los mismos bytes, está bien generado y solo faltaba commitearlo. **Lo que se arregló es el procedimiento:** el paso 10 listaba dos archivos generados y son **tres**. Descartado sacar la instantánea del glob: tocaría el camino publicado por 80 bytes |
| ⚪ | 25 | **H-019** · **El disparador y el defecto son dos cosas distintas, y solo una se resolvió.** La carga del módulo 3 murió con un **7403 de autenticación en la primera llamada de una terminal recién abierta**; se abre una terminal por carga y entre carga y carga pasan días, así que la sesión guardada de wrangler llega fría. **Resuelto con un paso 0** —`wrangler whoami`, de solo lectura, que despierta y pregunta a la vez; si falla, `login`—. **El defecto sigue vivo y el login no lo toca:** el guion no supo leer la respuesta porque venía como sobre de error con `notes: [...]`. **El 7403 fue amable porque falló ANTES de escribir; otro fallo de la nube** —límite de tasa, caída, red cortada a mitad— **daría el mismo `SIN VEREDICTO` con la carga quizá ya dentro**. Resolver el login **tapa el síntoma sin arreglar el defecto**, y además hace desaparecer el aviso de que el defecto existe. Anotado y no resuelto, por decisión del autor el 2026-09-10 |
| 🟢 | 25 | **«Mira la base antes de repetir» ahora dice cómo.** El mensaje de `SIN VEREDICTO` de `administrar-banco.mjs` pedía mirar la base y no daba el comando. En la carga del módulo 3 el autor no lo tenía a mano en ese momento y **repitió a ciegas; salió bien por suerte y no por método**. Ahora imprime el comando exacto de `comprobar-carga.mjs` con el módulo del encargo —sacado del sello de procedencia, no supuesto— y la base contra la que se estaba hablando. Provocado el 2026-09-10. **La lección vale más allá del mensaje: uno que pide algo sin decir cómo hacerlo se desobedece**, y no por descuido — se desobedece porque obedecerlo cuesta más que arriesgarse, justo cuando uno está nervioso porque algo acaba de fallar |
| ⚪ | 25 | **H-004 · segundo dato: la posición 2 concentra ~40% en los dos lotes.** Módulo 2 dio `23 / 40 / 29 / 8`; módulo 3 dio `16 / 39 / 30 / 15`. **Dos lotes independientes con casi el mismo número empieza a parecer sesgo del material y no casualidad**, y son bancos escritos por manos distintas en momentos distintos. **Todavía no se afirma —faltan cinco módulos—**, pero la expectativa cambió: hasta el módulo 2 lo razonable era suponer casualidad; desde el 3 lo razonable es suponer sesgo y esperar que los siguientes lo desmientan. No cambia qué se hace: H-004 se informa y no se corrige. Lo que la serie permitiría, si se sostiene, es **decirlo** — que un banco de práctica advierta que su material de origen tiene sesgo de posición es información útil para quien estudia con él |
| 🟢 | 25 | **ADR-029** · **Una alternativa imprecisa pero fiel al examen se conserva; solo se corrige la que enseña una regla falsa.** Establecida por el autor el 2026-09-10 al resolver las cuatro dudas del módulo 3, **como regla del proyecto y no como respuesta a un caso**. El examen real de Talento Digital afirma cosas de forma tajante, así que una alternativa dura pero fiel se conserva y **el matiz va en la justificación**; se corrige el texto solo cuando la alternativa correcta **afirma una regla falsa que el alumno va a aplicar mal en otra pregunta**, que no es imprecisión de lenguaje sino contenido equivocado. **La pregunta que decide no es «¿está bien dicho?» sino «¿qué se lleva el alumno si memoriza esta alternativa?».** Extiende ADR-017, que se ocupaba de los distractores: esta se ocupa de la **correcta**, que es la única que el alumno se lleva aprendida. Corregidas bajo la regla: `m02#30` y `m03#10`. Conservadas con el matiz: `m03#6`, `m03#32`, `m03#17` |
| 🟢 | 25 | **m03#10 corregida: la alternativa correcta explicaba el resultado con una regla falsa.** Decía que `[] == false` da `true` «debido a la coerción implícita de tipos hacia **booleanos**», y `==` no convierte a booleano: convierte a **número** —`false` a `0`, `[]` a `""` y de ahí a `0`—. El resultado que afirmaba era correcto y el motivo, falso. **La prueba de que importa:** `[] == false` da `true` mientras `[] ? 1 : 2` da `1`, porque como condición el arreglo vacío es verdadero; quien memorice «hacia booleanos» aplica mal la regla en la pregunta siguiente. Corregida a «hacia números», aprobada por el autor el 2026-09-10 |
| ⚪ | 25 | **Las correcciones del módulo 3 son de otra clase que las del 2, y conviene mirarlo con los cinco módulos que faltan.** En el módulo 2, **cuatro de las cinco** correcciones fueron de **enunciado**; en el módulo 3 la única es de una **alternativa correcta**. No cambia el procedimiento —ADR-028 obliga a reconvertir y recomprobar el módulo entero en los dos casos— pero **corregir la correcta mueve el texto en que ancla el comprobador de conversión**, así que la reconversión deja de ser una formalidad: sin ella el módulo quedaría comprobado contra un texto que ya no existe. Anotado para ver, al llegar al módulo 8, si la proporción dice algo sobre los bancos de origen o fue casualidad de estos dos lotes |
| 🟢 | 25 | **ADR-028 se disparó sola por primera vez, y no provocada.** Al corregir `m03#10` el 2026-09-10, `comprobar-conversion.mjs` respondió `SIN VEREDICTO · El encargo no corresponde a sus orígenes` **nombrando exactamente los dos orígenes que se habían movido** —`modulo-0N.json` y `correcciones-de-enunciado.json`— y dejando los otros dos con su huella intacta. Es la diferencia entre una comprobación provocada en un ensayo y una que hace su trabajo en el uso real. Reconvertido y recomprobado el módulo entero: `COMPROBADO`, con `correcciones registradas 1 · contrastadas contra el archivo de origen` |
| 🟢 | 25 | **El documento de revisión conservó 57 aprobaciones y devolvió a cero exactamente las 4 que cambiaron.** Comprobado el 2026-09-10 con material real: el autor había aprobado 57 de 61, se corrigió una alternativa y se reescribieron cuatro justificaciones, y al regenerar el documento salió `aprobadas 57 · SIN APROBAR 4`, listando `m03#6`, `m03#10`, `m03#17` y `m03#32`. **Es lo que hace tolerable la regla de ADR-028:** reconvertir el módulo entero no obliga a volver a leer 61 justificaciones, solo las que de verdad cambiaron |
| 🟢 | 25 | **ADR-028** · **Una corrección aprobada obliga a reconvertir y recomprobar el módulo entero, aparezca cuando aparezca.** Decidido por el autor el 2026-09-09, incluso después de haber aprobado el documento de justificaciones. **Motivo:** el costo de reconvertir es que un guion vuelva a correr; el de publicar un enunciado con premisa falsa **lo paga un estudiante leyéndolo**, estudiando para una certificación. No hay punto de corte que justifique cargar algo que sabemos malo. **La condición que la decisión se pone a sí misma:** que la reconversión sea **automática y comprobada**, no un gesto que se pueda olvidar. Implementada con `scripts/procedencia.mjs`: el encargo se sella al convertir con la huella de sus cuatro orígenes —el JSON del módulo entero, y **sólo las entradas de ese módulo** de `cuestionario.js`, `retiradas.json` y `correcciones-de-enunciado.json`— y **tres pasos lo cotejan y se niegan**: comprobar-conversion, aplicar-justificaciones y administrar-banco, que es la última puerta antes de escribir. **Provocadas las tres** el 2026-09-09 añadiendo una corrección de prueba: las tres se negaron nombrando cuál origen se movió, y la carga no escribió nada |
| 🟢 | 25 | **El procedimiento de un lote, escrito de principio a fin ANTES de convertir el módulo 3.** Once pasos más la rama de corrección, en `iteracion-25`. **Escrito antes a propósito, por decisión del autor:** si se escribiera después de convertir, saldría un procedimiento que describe el módulo 3 en vez de uno que sirva del 4 al 8; escribirlo antes lo obliga a ser general, y el módulo 3 pasa a ser su primera prueba en vez de su molde. **Arregla el hallazgo de fondo del módulo 2: la rama más cara —qué hacer cuando aparece una corrección— vivía en la memoria de quien lo había hecho**, y por eso está DENTRO de la lista y no como nota al pie. Fueron 5 correcciones de 52 preguntas, casi el 10%: no es un caso raro, es parte del trabajo |
| 🟢 | 25 | **El ensayo local pasa a ser un paso obligatorio, no una buena costumbre.** `scripts/ensayo-local.mjs <N>` vacía el banco local, carga el lote con la herramienta de verdad y le corre encima **el mismo comprobador que después se corre contra producción, con el mismo comando salvo el destino**. Es un ensayo general gratis del comando caro. En el módulo 2 esto se hizo, pero **para probar la herramienta y no como paso**, y esa diferencia importa: lo que no está escrito como paso se salta el día que hay prisa, que es justo cuando conviene haberlo hecho. Comprobado el 2026-09-09 con el lote del módulo 2: `ENSAYO CORRECTO`, código 0 |
| 🟢 | 25 | **El fixture local deja de hacerse copiando archivos.** El mismo guion reconstruye el banco de juguete desde `d1/ejemplo-banco.sql` —versionado y repetible— en vez de restaurar copiando los `.sqlite`, `-shm` y `-wal` por encima, que es lo que se hizo a mano el 2026-09-09 y es la hipótesis de H-029. Reconstruir desde SQL no puede dejar la base en un estado que el proceso siguiente no espere, porque no toca el motor por debajo: **le habla** |
| 🟢 | 25 | **H-030** · **El respaldo del banco no tenía banco dentro, y llevaba seis días así.** `d1/respaldo-banco.sql`, versionado y designado por ADR-014 como el respaldo del banco, contenía **527 bytes de `prueba_tuberia`** —la tabla de ensayo de la iteración 12, exportada el 2026-09-03— y **ni una sola pregunta**: no estaba desactualizado, era el volcado de otra cosa. **Causa:** el «un solo paso produce las dos cosas, o no produce ninguna» de ADR-023 **nunca se implementó**; eran dos comandos sueltos y un recordatorio impreso entre medio. La propia ADR había predicho el desenlace —«separarlas es garantizar que una se quede atrás»— y se equivocó sólo en cuál: apostaba a la instantánea por muda, y se quedó atrás el respaldo. **Corregido con `scripts/publicar-banco.mjs`** (`npm run datos:publicar`): prepara las dos mitades en temporales y **sólo instala si las dos salieron bien**, comprueba que el respaldo **traiga banco dentro** —la comprobación que faltaba—, que el sello diga `nube`, y que la instantánea no publique más preguntas de las que el respaldo contiene. Provocados sus cuatro sabotajes y sus tres rechazos, **con huella de los dos archivos antes y después en los cuatro**. Detalle en la auditoría |
| 🟢 | 25 | **Quinta vez del patrón de H-023, y dentro de la herramienta escrita para cerrar ADR-023.** El sabotaje `sello-local` de `publicar-banco.mjs` comprobaba el rechazo de una instantánea con sello `local`, y su condición estaba escrita como `if (remoto && …)` — pero **los sabotajes están prohibidos con destino remoto**, a propósito, para no tocar la cuenta. Era una comprobación **imposible de ejercitar**, que habría quedado marcada como probada sin haberlo estado. Corregido separando «se pidió el sello de la nube» de «se habla con la nube»: son dos cosas distintas y sólo la segunda toca la cuenta |
| 🟢 | 25 | **La protección de «no pisar una instantánea de la nube con una local» sobrevivió al enrutado, y se comprobó.** Para que el paso único pudiera preparar la instantánea aparte hubo que añadir `--salida=` a `generar-instantanea.mjs`, y eso podía **apagar sola** esa protección: si el generador mirara el archivo temporal, no habría nada anterior que proteger. Se separó el archivo canónico del de escritura y **se provocó**: con el sello canónico puesto en `nube`, una corrida local responde `ME NIEGO A PISAR LA INSTANTANEA` y el paso único informa que no tocó nada |
| 🟢 | 25 | **Módulo 2 comprobado en producción: cinco criterios de nivel 1 cerrados con evidencia.** Corrida por el autor el 2026-09-09 con `scripts/comprobar-carga.mjs` contra producción: `CARGA COMPROBADA`, código 0, uuid `cff1686b-…` cotejado antes de leer. `json_2026 39 = 39`, `js_2026 13 = 13`, 52 activas, `justificacion IS NULL 0`, `dificultad IS NULL 52 de 52`, las 3 retiradas del módulo ausentes, y **`orden_fijo = 1` en la id 50 con `static/js/data/cuestionario.js` todavía en el árbol** — la mitad que da valor a la otra, porque en cuanto se retire el `.js` la comprobación deja de poder repetirse contra su origen. Quedan dos de nivel 1: el bloque de ADR-023 y ver el módulo en el sitio publicado |
| ⚪ | 25 | **H-004 · La posición 2 concentra el doble que la 4 en el primer lote, y todavía no se sabe si es sesgo o casualidad.** Reparto de la correcta en las 52 del módulo 2: **23% / 40% / 29% / 8%**, contra el 25% de un reparto parejo. Con un solo módulo **no se puede distinguir un sesgo del material de una casualidad de estas 52**; un lote de ese tamaño admite desviaciones así por azar. Con los siete se va a ver: si el patrón se repite lote tras lote es del material, y si baila eran estas 52. Por eso el reparto se informa en **cada** módulo y no sólo al final — **la serie es el dato, no el promedio**. Lo que no cambia: H-004 se informa y no se corrige, porque reordenar las alternativas para cuadrar un histograma sería falsear el material de origen |
| 🟡 | 25 | **H-029** · **El comprobador de restricciones falló una vez y no se ha vuelto a reproducir.** `npm run verificar` informó `RESTRICCION CAIDA · Indice parcial: una sola correcta por pregunta`, y acto seguido el índice resultó existir con su definición correcta, `npm run probar:restricciones` dio las nueve en pie con la huella idéntica antes y después, y la corrida completa siguiente dio `restricciones OK`. **Visto una vez, no reproducido, hipótesis sin confirmar** — y anotado con esas palabras a propósito, por decisión del autor: **un fallo intermitente en el comprobador de restricciones es de los que vuelven**, y cerrarlo como incidente resuelto enterraría la única pista. Hipótesis, sin provocar: ocurrió justo después de restaurar los `.sqlite`/`-shm`/`-wal` de la base local copiándolos por encima. **No se cierra hasta que alguien lo provoque** **Intento de provocación, 2026-09-09: NO SE REPRODUJO.** `ensayo-local.mjs --provocar-h029=8` restauró la base de las dos maneras y corrió el comprobador tras cada una: **0 fallos de 8 copiando archivos y 0 de 8 reconstruyendo desde SQL**. Ocho vueltas no prueban que no ocurra, así que la hipótesis del `-wal` queda **sin apoyo empírico** en vez de descartada, y el hallazgo sigue en amarillo — pero ya no vacío. **Se descartó de paso una hipótesis alternativa cómoda:** el andamiaje dejado por una corrida anterior produce `SIN VEREDICTO` código 2, que es la respuesta correcta, y no el `RESTRICCION CAIDA` observado. **Y queda escrito qué mirar si vuelve:** el fallo sólo es posible si en ese instante ninguna alternativa de la 9001 tenía `es_correcta = 1`, así que lo primero —antes de tocar nada— es `SELECT name FROM sqlite_master WHERE type='index'`, la única consulta que deja de poder hacerse en cuanto alguien «arregla» la base. El disparador sospechoso ya no está en el procedimiento: el fixture reconstruye desde SQL. **Eso no lo cierra:** cerrarlo exigiría explicar el fallo, no dejar de rozarlo. |
| 🟢 | 25 | **ADR-026** · **Cómo trabaja el autor queda escrito, en vez de suponerse.** Toda carga de cuestionarios la hace él desde su computador, revisando formato e integridad antes; no hay cargas automáticas ni desde otro equipo. Estaba escrito *quién* puede hablar con la cuenta (ADR-015) y *con qué* se edita el banco (ADR-025), pero no **cómo se trabaja de hecho**, y esa diferencia importa al dimensionar barreras: una amenaza que sólo se materializa con una carga desatendida no aplica aquí — pero no aplicaba **por costumbre**, que es como se dejan de cumplir las cosas |
| 🟢 | 25 | **ADR-027** · **El patrón del enganche no se amplía; la regla es que cada guion se defienda solo.** Decidido por el autor el 2026-09-09: todo guion que hable con la nube lleva **su propia** barrera de ADR-015 y **se comprueba provocándola**. Motivo: el patrón del enganche siempre va a ir por detrás de los guiones nuevos, y perseguirlo da una sensación de cobertura que una lista de nombres no sostiene |
| 🟢 | 25 | **La capa 2 cubre menos de lo que nadie creía, y se supo midiendo.** Se le dieron al enganche las seis líneas de comando reales que hablan con la nube: **bloquea dos**. No ve `volcar-contenido.mjs` ni `comprobar-carga.mjs` —que era lo sabido—, **ni `administrar-banco.mjs`, que es la herramienta de escritura**, ni `generar-instantanea.mjs` invocado con `node`; ese último se bloquea por `npm run datos:instantanea` y pasa como `node scripts/generar-instantanea.mjs`, o sea que **el mismo guion pasa o no según cómo se lo escriba** (H-011 otra vez). Esto **refuerza** ADR-027 en vez de contradecirla: la lista ya iba por detrás de los guiones viejos. Los cuatro tienen barrera propia; dos **provocadas**, y las de `administrar-banco` y `generar-instantanea` **leídas y no provocadas**, que por H-023 no cuenta — las provoca el autor con dos comandos que están en `90-manual/barrera-adr-015.md` |
| 🟢 | 25 | **H-027** · **PATRON, cuarta vez: la comprobación de retiradas miraba un campo que no existe.** El bloque de `comprobar-carga.mjs` que verifica que ninguna de las 37 retiradas se haya cargado leía `r.enunciado ?? r.q` sobre la entrada de `retiradas.json`, y ahí no está el texto: está anidado en `r.pregunta`, como `enunciado` en el banco nuevo y como `q` en el viejo. Obtenía `undefined` **siempre**, salía por el `continue` de guarda y no comparaba ni una vez, informando «ninguna en la base» pasara lo que pasara. **Apareció escribiendo el sabotaje que la obliga a decir «no»**, que reventó con `TypeError` sobre ese mismo `undefined`: es la primera vez que la regla de H-023 destapa algo en vez de sólo describirlo. **Corregido** leyendo el campo bueno y, sobre todo, dejando de saltarse en silencio lo que no entiende: una entrada sin texto donde se lo busca ahora se informa como `RETIRADA ILEGIBLE` y cuenta como problema. **De paso se acotó un refuerzo que habría acusado en falso:** el cotejo por número sirve para `json_2026`, cuyo `numero` es estable, y no para `js_2026`, cuya `posicion_original` es anterior a los retiros del 2026-09-04 mientras que el `numero_origen` cargado es la posición de hoy. Detalle en la auditoría |
| 🟢 | 25 | **El detector de justificaciones de relleno gritaba con material bueno.** La otra cara de H-027, encontrada en el mismo acto: marcó **ocho justificaciones legítimas** del módulo 2 porque buscaba las palabras en cualquier parte del texto y, con la bandera `i`, el patrón `TODO` cazaba la palabra española «todo». **Una comprobación que nunca puede decir «no» y una que dice «no» con material bueno son el mismo error**: no miden lo que dicen medir. La segunda no es inofensiva, porque un detector que grita con material bueno enseña a ignorarlo. **Corregido** anclando el patrón al principio —un relleno no *contiene* «pendiente», *empieza* por ahí— y con un largo mínimo **medido**: la justificación más corta de las 52 tiene 128 caracteres y «Pendiente de redacción» tiene 22, así que 60 no roza ninguna escrita de verdad |
| 🟢 | 25 | **H-028** · **El cargador del banco estaba roto en el árbol y sólo se notaba al usarlo.** `administrar-banco.mjs insertar` moría con `ReferenceError: abrirRegistro is not defined`: el arreglo de H-026 añadió la llamada y no su `import`. La herramienta con la que se cargan los seis módulos que faltan estaba inservible, y se habría descubierto en medio del procedimiento de carga del módulo 3 en vez de antes. **Corregido** y comprobado cargando las 52 del módulo 2 contra la base local, `0 -> 52`. **Lección:** un error de referencia en un módulo ES no aparece hasta que se ejecuta esa línea, y **ninguna comprobación del proyecto lanza los verbos de escritura**: `npm run verificar` mira la barrera, el CSS y el escapado, y ninguna de las tres carga nada |
| 🟢 | 25 | **Los duplicados por redacción distinta son esperados, y a veces deseables.** Decidido por el autor el 2026-09-09 sobre un par concreto del primer lote: `m02#18` y `M2-4` preguntan el mismo hecho —las 12 columnas del grid de Bootstrap— con redacciones distintas, y **las dos se cargan**. Motivo: los alumnos que rindieron la certificación dan testimonio de que **el examen real repite el mismo hecho con redacciones distintas**, así que un par así es fidelidad al examen y no un defecto del banco. **Cambia un supuesto:** «368 no significa sin duplicados» dejó de ser una limitación pendiente de cerrar y pasó a ser una característica declarada; la sección «Lo que esta iteración no puede afirmar» se reescribió por eso |
| ⚪ | 25 | **Deuda: los 37 retiros del 2026-09-04 se decidieron bajo el supuesto contrario.** Se aprobaron dando por sentado que un duplicado es un defecto, y ese supuesto cambió el 2026-09-09. **Alguno pudo haber sido un duplicado legítimo.** Los 11 empates son los más sospechosos: se resolvieron «por consistencia de formato» entre dos preguntas de calidad equivalente, que es justo el caso en que hoy se conservarían las dos. **No se revisa en la iteración 25 a propósito**: reabrir 37 decisiones editoriales a mitad de una carga por lotes mezcla dos trabajos y pone en riesgo el que está en curso. Nada se perdió, `retiradas.json` las tiene íntegras con su par y su razón |
| ⚪ | 23 | **El procedimiento del manual junta exportación e instantánea en un solo bloque (ADR-023).** Editar, exportar `d1/respaldo-banco.sql`, regenerar la instantánea, publicar. Si el procedimiento no las junta, ADR-014 y ADR-008 quedan incumplidas sin que nadie lo note, y la que se queda atrás será la instantánea: el respaldo duele de forma ruidosa y la instantánea de forma muda |
| ⚪ | 24 | **Deuda viva: nada comprueba que la instantánea y el banco no hayan divergido.** Abierta el 2026-09-08, a raíz del hallazgo de la `id 11`. `npm run verificar` mira la barrera, el CSS y el escapado; **ninguna de las tres compara `static/js/data/instantanea-banco.js` con la base de la que dice salir.** Esa contaminación se cazó a mano, comparando dos números que nadie estaba obligado a mirar, y solo porque un recorrido en frío la puso al lado del banco versionado. Es el mismo patrón que H-014 dejó escrito para la barrera: lo que no se comprueba solo, se cae en silencio, y la instantánea se cae de la forma más muda de todas, porque únicamente se mira cuando la capa de datos ya falló. **Qué haría falta**, sin implementarlo ahora: que el sello declare contra qué contenido se generó y que algo lo contraste —contra `d1/ejemplo-banco.sql` en local, contra el banco real en la nube— y avise si el archivo versionado dejó de corresponder. Pensarlo junto al criterio de ADR-023: hoy solo el autor puede generar la instantánea buena, así que el comprobador tiene que poder dar «no pude verificar» sin que eso valga como aprobado (H-013) |
| ⚪ | 24 | **El escapado no queda probado a escala hasta esta iteración (ADR-024).** La 22 se verifica con diez filas de juguete. Con las 368 cargadas hay que recorrer la página entera buscando texto salido de su tarjeta: el banco real trae **57 preguntas con comillas invertidas y 46 con comillas dobles escapadas**. Es un criterio que la 24 hereda sin haberlo pedido |
| ⚪ | 22 | Extremos de lectura en el Worker, con validación |
| ⚪ | 22 | Instantánea local de respaldo (ADR-008) |
| ⚪ | 22 | Aviso visible al estudiante cuando se usa el respaldo |
| ⚪ | 22 | Reforzar el escapado: el contenido pasa a ser de origen externo |
| ❌ | 23 | **H-021 · RETIRADO el mismo día, era un error de medición mío, no un hallazgo.** Se anotó el 2026-09-08 que wrangler devolvía código de salida **0** llevando un `[ERROR]` dentro. **Es falso.** El `0` era el código de salida de `tail`: en `wrangler … 2>&1 \| tail -12`, `$?` devuelve el del **último** comando de la tubería, no el de wrangler. Medido de nuevo sin tubería, wrangler devuelve **1** en el rechazo de la transacción, **1** en el fallo de restricción y **0** en el éxito, que es exactamente lo correcto. Queda escrito en vez de borrado porque la trampa es real y este proyecto ya lleva tres hallazgos girando en torno a códigos de salida (H-011, H-013, H-016): **`$?` después de una tubería no mide lo que uno cree que mide**, y una medición mal hecha produce un hallazgo con toda la apariencia de estar comprobado. Lo que **sí** justifica que la herramienta no se fíe del código de salida sigue siendo **H-016**, que ya estaba catalogado y no depende de esto |
| 🟢 | 23 | **D1 rechaza `BEGIN TRANSACTION`, y el todo o nada sale de no escribirlo.** Comprobado el 2026-09-08 antes de implementar: con `BEGIN`/`COMMIT` el lote entero se rechaza; **sin** ellos, `wrangler d1 execute --file` aplica el archivo como lote atómico. Provocado sobre una tabla de juguete con la base sembrada: un lote de tres filas —dos válidas y una que viola un `UNIQUE`— dejó la base con las dos filas originales, y las dos válidas se insertaron y **se revirtieron**. La regla 1 de ADR-025 estaba bien en su efecto y mal en su mecanismo; corregida en la ADR, porque quien la leyera iba a escribir `BEGIN` y romper el lote. Comprobado en **local**: el mismo comportamiento en remoto es de la iteración 24 |
| 🟢 | 23 | **Las reglas de escritura salieron del camino publicado, y el rango de módulos quedó definido una sola vez.** Cambio aparte del 2026-09-08, después de commitear la iteración. `functions/api/_validacion.js` viaja al Worker que sirve preguntas a los estudiantes, y llevaba dentro 71 líneas de código que sólo usa una herramienta local. **Se midió antes de mover nada:** compilado el paquete de Cloudflare, el empaquetador ya descartaba esa mitad entera, así que en producción no viajaba. Se partió igual, porque el motivo no era el peso sino que un archivo del camino publicado contaba una historia que no era la suya. Ahora `scripts/validacion-de-escritura.mjs` tiene las reglas de escritura e importa `motivosDeContenido()` desde `_validacion.js`, que baja de 326 a 193 líneas. **El paquete compilado quedó del mismo tamaño exacto, 23.737 bytes**, y sin rastro de `validarParaEscritura`, `DIFICULTADES`, `ORIGENES` ni `LETRAS` —ahora por construcción y no por poda del empaquetador—. El criterio de «las mismas reglas en los dos caminos» se volvió a provocar **después** del corte: cambiar `ALTERNATIVAS_ESPERADAS` de 4 a 3 en un archivo movió los dos caminos con el mismo texto, ahora cruzando la frontera entre `functions/` y `scripts/`. De paso se eliminó la duplicación que el corte dejó a la vista: `MODULO_MINIMO` y `MODULO_MAXIMO` estaban definidos en `preguntas.js` y en `_validacion.js`, y dos definiciones del mismo rango no fallan al divergir, se quedan calladas |
| 🟢 | 23 | Decidir el mecanismo de administración del contenido. **ADR-025**: herramienta local de línea de comandos, sin panel y sin extremo de escritura. Su regla 1 se corrigió el 2026-09-08 al comprobar que D1 rechaza `BEGIN TRANSACTION` y que el todo o nada sale de no escribirlo |
| 🟢 | 23 | **La herramienta no alcanza producción por descuido: los tres rechazos provocados, ninguno escribió nada.** Base no declarada (H-015) → `NO SE APLICO · BASE NO DECLARADA`, provocado por Claude Code. Modo remoto sin permiso → `ME NIEGO A CORRER ESTO · ADR-015`, código 1. Modo remoto sin nombrar la base → `NOMBRA LA BASE`, código 1. **Los dos últimos los provocó el autor el 2026-09-08, y el motivo importa:** el enganche `PreToolUse` de ADR-015 ve el argumento de la nube en la línea de comandos y frena a Claude Code **antes** de que la herramienta llegue a ejecutarse. Se podía esquivar metiendo el comando dentro de un archivo —el propio enganche lo advierte— y no se hizo. **No es una limitación: es la defensa en capas funcionando**, la capa 2 deteniendo al agente antes de llegar a la capa que se venía a comprobar. En el terminal del autor la barrera no aplica, así que ahí sí se pudo empujar contra la puerta de la herramienta, y aguantó |
| 🟢 | 23 | Implementarlo cumpliendo ADR-009: sin escritura pública. `scripts/administrar-banco.mjs`, con tres verbos —`revisar`, `insertar`, `actualizar`—. Ningún extremo nuevo en el Worker: la capa publicada sigue siendo de solo lectura. Validación compartida con la lectura en `functions/api/_validacion.js`, comprobado cambiando una regla en un solo lugar y viéndola moverse en los dos caminos con el mismo texto |
| 🟢 | 23 | Carga por lotes, y **todo o nada demostrado con un fallo real**. Lote de tres con la del medio chocando contra `UNIQUE (enunciado)`: la base quedó con las mismas 60 filas y el mismo resumen `0df127ac28ca8bdb`, comparando el volcado de `pregunta` y `alternativa` campo a campo, no el mensaje de la herramienta |
| 🟢 | 23 | Regenerar la instantánea al editar, dentro del mismo acto (regla 5 de ADR-025). Comprobado por el sello antes y después de una corrección. La herramienta avisa en voz alta de que esa copia salió de la base local y no es la que se publica |
| ⚪ | 24 | **Aplicar la migración 001 en las dos bases de la nube, pruebas y producción.** Era el paso 5 de la iteración 21 y **no se ejecutó**: queda aquí, junto a la carga del banco, porque crear el esquema en producción sin contenido que meterle no aporta nada y sí abre una ventana en la que la base publicada tiene tablas vacías. Los comandos están escritos en `90-manual/esquema-del-banco.md` y los ejecuta el autor. Arrastra consigo el borrado de `prueba_tuberia` en ambas bases |
| ⚪ | 24 | Transformar los cuestionarios de los siete módulos a un formato estructurado y verificado, previo al esquema |
| ⚪ | 24 | Cargar el banco transformado en D1, una vez definido el esquema de la iteración 21 |
| ⚪ | 24 | Escribir la justificación de cada pregunta |
| 🟢 | 24 | **Los distractores evidentemente descartables del banco nuevo se conservan a propósito. No es un defecto: es fidelidad al examen real** (decisión del autor, 2026-09-04). Al comparar los dos bancos apareció un patrón: el viejo compite contra alternativas que existen —`path`, `http`, `os`— y el nuevo inventa nombres que se descartan sin saber la materia —`path-directory (pd)`, `os-architecture`, `hasMultiple()`—. La tentación evidente es reescribirlos. **No se hace.** Los testimonios de alumnos que rindieron el examen real de certificación de Talento Digital coinciden en que ese examen trae bastantes alternativas evidentemente descartables. Un banco de práctica con distractores impecables entrena para un examen que no es el que van a rendir. La fidelidad al examen real pesa más que la calidad teórica del ítem. **Quien lea esto en el futuro y lo tome por una lista de errores, que se detenga: corregirlo empeora la simulación.** Si algún día cambia el examen real, cambia la premisa y entonces se revisa; mientras tanto se deja como está. **No cubre los ítems con dos respuestas correctas**, que sí son errores: ver el par `m07#21 ↔ M7·8` en la iteración 24. **Publicado como ADR-017** |
| 🟢 | 24 | **Retiros por solapamiento aplicados: el banco quedó en 368.** 34 pares más el cuarteto de atomicidad, 37 preguntas retiradas —15 del banco nuevo y 22 del viejo—, aprobadas una por una por el autor el 2026-09-04. Nada se borró: están en `cuestionarios/retiradas.json` con su origen y su razón. **El banco nuevo no se renumeró**, porque `numero` es la referencia que usa todo el plan. Lista operativa en la iteración 24 |
| ⚪ | 24 | **`scripts/build-cuestionario.py` no se puede ejecutar: su entrada ya no existe.** Lee de `/mnt/user-data/uploads`, el directorio de subida de una sesión de Claude web, y los markdown de origen nunca entraron al repositorio. Consecuencia: `static/js/data/cuestionario.js` es un archivo generado que hoy es la **única copia** de su propio contenido, y hubo que editarlo a mano para aplicar los retiros. Esta iteración retira los dos archivos, con lo que el problema se cierra solo; si por lo que sea sobrevivieran, hay que decidir qué pasa con un generador sin entrada |
| ⚪ | 24 | **Advertencia sobre los números de solapamiento: retirar 37 preguntas no deja el banco sin duplicados.** Los 82 pares candidatos que produjo `npm run informe-banco` salieron de **parecido de redacción** —trigramas compartidos y palabras en común—, no de entender el contenido. Dos preguntas sobre el mismo punto escritas con vocabulario distinto **no aparecen en esa lista, y nadie las ha visto**: ni el script, ni el autor, ni Claude Code. Quien lea más adelante «el banco quedó en 368 preguntas sin duplicados» tiene que leer «368 preguntas sin los duplicados que el parecido de redacción alcanzó a detectar». Cerrar el hueco de verdad exige una revisión por tema, pregunta por pregunta, que hoy no está planificada ni presupuestada |
| ⚪ | 24 | **El contador de la portada del cuestionario está escrito a mano y no lo actualiza nadie.** Dice «105 preguntas · 7 módulos» en `cuestionario.html` línea 60, y lo mismo en el `<meta name="description">` de la línea 7. El 2026-09-08, con el sitio publicado sirviendo la instantánea de respaldo, la pantalla mostraba «105 preguntas» arriba a la izquierda y «copia guardada en el sitio (8 preguntas)» en el pie, al mismo tiempo. Cuando se carguen las 368 va a seguir diciendo 105. El número tiene que salir del dato que se está mostrando, o desaparecer |
| ⚪ | 24 | **`/api/estado` dice que la base responde bien aunque el camino real esté caído.** `functions/api/estado.js` línea 36 hace `SELECT 1 AS vivo` y de ahí concluye `consulta_d1: "correcta"`. Esa consulta pasa igual con la base sin esquema: el 2026-09-08 en producción, `/api/estado` devolvió 200 con `consulta_d1: "correcta"` y `vacio: false` mientras `/api/preguntas` devolvía 503 con `FALLO_CONSULTA`. El único extremo de diagnóstico del sitio informaba que todo estaba bien mientras el sitio servía el respaldo. Una comprobación que no puede fallar no comprueba nada |

## Épica 30 · Cuestionario

| Estado | Iteración | Tarea |
|---|---|---|
| ⚪ | 31 | Consumir el banco completo desde la nueva fuente |
| ⚪ | 31 | Renderizado por módulo para no dibujar 300 preguntas de golpe |
| ⚪ | 32 | Barras de progreso en formato horizontal, a todo el ancho del panel |
| ⚪ | 32 | Índice de módulos con avance individual |
| ⚪ | 33 | Guardar el avance en el navegador y poder retomarlo |
| ⚪ | 33 | Modo repaso: reintentar solo las preguntas falladas |
| ⚪ | 33 | Mostrar la justificación al responder |

## Épica 40 · Simulacro de examen

| Estado | Iteración | Tarea |
|---|---|---|
| ⚪ | 41 | Pantalla de presentación con reglas y botón de inicio |
| ⚪ | 41 | Motor de sesión: selección de 120 preguntas y estado del intento |
| ⚪ | 41 | **Diez pares de preguntas hermanas que el motor no debería sacar en el mismo intento.** No son duplicados —cada una evalúa algo distinto y por eso ninguna se retira— pero se rozan tanto que verlas seguidas le regala la respuesta al estudiante. Eran once; uno se disolvió al retirarse `M8·11`. **Las posiciones del banco viejo se movieron con los retiros y volverán a moverse**: la tabla hay que reanclarla a los identificadores de D1 en cuanto existan. Lista completa en la iteración 41, sección «Preguntas hermanas» |
| ⚪ | 42 | Cronómetro total de 60 minutos |
| ⚪ | 42 | Cronómetro de 30 segundos por pregunta, con avance automático |
| ⚪ | 43 | Una pregunta a la vez, con botón de avance manual |
| ⚪ | 44 | Resumen de resultados con desglose por módulo |
| ⚪ | 44 | **La pantalla de resultados no puede usar vocabulario de certificación (ADR-022).** Ni «puntaje oficial», ni «nota», ni «calificación», ni «aprobado/reprobado». Sí aciertos, desglose por módulo y tiempo. **Es una restricción de lenguaje, no una advertencia en pantalla**: la decisión de que el simulacro no es instrumento de evaluación con validez es interna y no se le muestra al estudiante. Está escrita como lista de palabras porque quien redacte esa pantalla puede no haber leído la ADR |
| ⚪ | 45 | Identidad visual propia del simulacro |

## Épica 50 · Endurecimiento y observabilidad

| Estado | Iteración | Tarea |
|---|---|---|
| ⚪ | 51 | Cabeceras de seguridad y reglas de protección |
| ⚪ | 51 | Límites de tasa en el Worker de lectura |
| ⚪ | 51 | Verificar que no hay escritura alcanzable públicamente |
| ⚪ | 52 | Métricas de tráfico sin rastreo de personas |
| ⚪ | 53 | Política de caché del sitio y de las respuestas del Worker |
| ⚪ | 53 | Vigilancia de los límites de uso del plan gratuito |

---

## Sin asignar

Tareas necesarias que todavía no pertenecen a ninguna iteración. Se rescatan cuando
se planifique la iteración que las incluya.

| Estado | Origen | Tarea |
|---|---|---|
| 🟢 | Iteración 25 · **H-025** | **HECHA el 2026-09-09: la importación es atómica.** 255 sentencias emitidas antes del choque, ninguna sobrevivió, `fb6908910e0ac450` antes y después. La corrió el autor contra pruebas. Evidencia en el bloque «Resultado» de H-025. *(Enunciado original, que queda como estaba:)* **Ensayar el todo o nada por el camino de importación, contra la base de pruebas de la nube.** Abierta el 2026-09-09. La atomicidad está comprobada contra la base local con 312 sentencias y el fallo puesto al final, pero la base local **nunca toma el camino de importación**: ese camino es exclusivo de las escrituras contra la nube, y por él van las seis cargas que quedan. Hoy el único testigo es una frase que imprime wrangler prometiendo que la base vuelve a su estado, y una promesa del proveedor no es una comprobación (regla de H-023). **Qué haría falta:** el ensayo escrito paso a paso en `90-manual/ensayo-del-camino-de-importacion.md`, con sus dos instrumentos ya probados contra la base local. Lo corre el autor, contra pruebas, nunca contra la base publicada (ADR-015) |
| 🟢 | Iteración 25 · **H-026** | **CERRADO el 2026-09-09: `--registro` funciona en el terminal donde aparecía `stdout is not a tty`.** La corrida de `comprobar-carga.mjs` contra producción se lanzó con `--registro=evidencia-modulo-02.txt` desde el terminal del autor y el archivo salió completo: cabecera con fecha y argumentos, 59 líneas, y la línea de cierre `# fin del registro · CARGA COMPROBADA · codigo 0`. **Era la única propiedad que quedaba como razonamiento en vez de evidencia**, y se cerró sola en la primera corrida real, como estaba previsto. *(Enunciado original:)* **Confirmar que `--registro` funciona bajo winpty.** Abierta el 2026-09-09. El arreglo de H-026 se probó en un terminal donde winpty no intervenía, así que la propiedad que importa —que sin redirección winpty no tenga nada que objetar— es **razonamiento, no evidencia**. Lo cierra el autor la primera vez que corra una carga con `--registro` desde el terminal donde apareció `stdout is not a tty`: si el archivo trae la cabecera y la línea de cierre, está comprobado. No merece una tarea aparte, se cierra sola en la primera carga del módulo 3 |
| ⚪ | Iteración 25 · **H-025** | **`actualizar` levanta un proceso de wrangler por pregunta para resolver su id.** Abierta el 2026-09-09. Con 52 preguntas son 52 arranques: **148 segundos medidos en local**, y contra la nube cada uno suma además un viaje de red. No estorba a las seis cargas que vienen, porque son `insertar` y ese verbo no resuelve ids, pero deja inviable una corrección masiva por `actualizar`. **Qué haría falta:** resolver los ids de todo el lote en una sola consulta, con un `IN` sobre las ternas, en vez de una consulta por pregunta |
| ⚪ | Iteración 23 | **Un ejecutor de migraciones que lea la tabla `migracion` y aplique solo lo pendiente.** Abierta el 2026-09-08. Hoy las migraciones se aplican a mano, una por una, con un `npm run datos:migrar-00N` por archivo. Funciona con dos y deja de funcionar solo: cada archivo nuevo suma un comando que hay que recordar, en el orden correcto, y **la `002` no es repetible** —SQLite no admite `IF NOT EXISTS` en `ALTER TABLE ADD COLUMN`, así que la segunda corrida responde `duplicate column name`—. El fallo es seguro, porque el lote es atómico y no aplica ni registra nada, pero es ruido que enseña a ignorar errores. **Qué haría falta:** un guion que consulte `migracion`, aplique en orden los archivos de `d1/migraciones/` que no estén ahí, y no toque los que ya están. Con eso `npm run datos:migrar` vuelve a ser un solo comando repetible, como era cuando había una sola migración. Decidido posponerlo por el autor el 2026-09-08: es alcance nuevo y la iteración 23 no lo pedía |
| ⚪ | Iteración 23 | **No borrar la rama `antigravity` (`3f45da7`). No es basura: es la única copia del código que se usa de referencia para rehacer la iteración 23.** De ella salieron los 8 archivos que no pasaron a `main` el 2026-09-08 —`scripts/admin-db.mjs`, los tres `scripts/lib/db-*.mjs`, `d1/migraciones/002-fecha-modificacion.sql`, sus dos markdown y `modulo-02-antigravity.json`—, y de su documento hay que rescatar una ADR que quedó escrita fuera de `decisiones.md`. **Se borra cuando la 23 esté rehecha y verificada, no antes.** Decidido por Felipe Cuevas el 2026-09-08 |
| ⚪ | Auditoría | Archivo de licencia propia y atribución de terceros (Material Symbols es Apache 2.0 y exige atribución) |
| ⚪ | Auditoría | Metadatos para compartir en redes: imagen y descripción |
| ⚪ | Auditoría | Página 404 con la identidad del sitio |
| ⚪ | Auditoría | Auto-hospedar las tipografías y eliminar la dependencia externa |
| ⚪ | Auditoría | Semántica de formulario en las alternativas, para lectores de pantalla |
| ⚪ | Auditoría | Anunciar los cambios de estado a tecnologías de asistencia |
| ⚪ | Auditoría | No comunicar solo por color en las barras de progreso |
| ⚪ | Auditoría | Manejo del foco al reiniciar un cuestionario |
| ⚪ | Auditoría | Estilos de impresión del temario |
| ⚪ | Auditoría | Plantilla de issue para que los alumnos reporten preguntas erróneas |
| ⚪ | Auditoría | Comprobación automática de que el CSS compilado está al día |
| ❌ | ADR-007 | **Descartada el 2026-09-05 por ADR-022.** Texto original: «Evaluar si el Worker debe dejar de enviar la respuesta correcta al navegador durante el simulacro, y validar del lado del servidor». Motivo del descarte: la instantánea de respaldo incluye las respuestas correctas —sin ellas el modo degradado no puede corregir, y el público estudia desde el teléfono con conexión irregular—, y estando en un repositorio público, ocultarlas en el servidor no protege nada. Lo que se pierde está escrito en ADR-022: **el simulacro no puede garantizar que nadie vea las respuestas antes de responder**, y por lo tanto no es un instrumento de evaluación con validez |
| ⚪ | ADR-007 | Definir qué ocurre con el proyecto si se agotan los límites del plan gratuito |
| ⚪ | Iteración 12 | Cloudflare Pages responde **308** a `/cuestionario.html` y redirige a `/cuestionario`: quita la extensión por su cuenta. No rompe nada, porque el navegador sigue la redirección, pero todos los enlaces internos del sitio pagan un salto de más. Decidir si los enlaces pasan a escribirse sin extensión |
| ⚪ | Iteración 12 | La orden de construcción (`npm run build`) no se puede declarar en `wrangler.toml`: es el único ajuste de la publicación que el repositorio no cubre y que vive solo en el panel. Si alguien la vacía ahí, ningún archivo del repositorio lo impide. Evaluar si una comprobación posterior al despliegue puede detectarlo |
| 🟢 | — | **H-012** · Falsa alarma en un clon recien bajado. Resuelto por los dos lados: la comparación ignora los finales de línea, y `.gitattributes` hace que el checkout escriba LF. No hizo falta renormalizar: el historial ya estaba en LF |
| 🟢 | 21 | **H-014** · ADR-015 falló en su primera prueba real. Claude Code ejecutó un `wrangler --remote` que se autenticó y alcanzó la base de **producción** por el extremo `/import`, dos POST con 200. No modificó datos —era un `SELECT` y allá no hay tablas—, pero eso fue casualidad, no barrera. **La lección no es el guion que falló: es que la regla se comprueba mirando el comando, y el comando era `powershell -File correr.ps1`, donde no se ve ningún `--remote`.** Aprobadas por el autor e implementadas las cuatro capas técnicas: entorno sin credenciales (`XDG_CONFIG_HOME` desviada), enganche `PreToolUse`, el envoltorio negándose a `--remote`, y `verificar-barrera` como primer paso de `npm run verificar`. Siete estados provocados de verdad, incluida la barrera caída. **Cerrado el 2026-09-05:** el paso que faltaba —que la sesión cargara el enganche— ya ocurrió, y las cuatro condiciones se comprobaron en verde desde dentro de Claude Code, con el testigo del enganche a **0 s**, que es lo que distingue «declarado» de «vivo» |
| 🟢 | 21 | **La barrera de ADR-015 vive en `~/.claude/settings.json`, no en el repositorio.** Comprobado que el bloque `env` de los ajustes de proyecto no llega al entorno ni tras reiniciar; probablemente Claude Code lo impida a propósito, y con buen criterio. Segundo motivo: la regla es sobre Claude Code, no sobre este repositorio. Aceptado por el autor que aplique a todas sus sesiones. ADR-015 enmendada y `90-manual/barrera-adr-015.md` escrito con el procedimiento completo. El enganche y los guiones sí quedan versionados: la barrera es mitad repositorio, mitad equipo |
| 🟢 | 21 | **La capa 4 no distinguía «enganche declarado» de «enganche vivo», y esa diferencia se cobró un día entero.** El archivo era correcto, el guion era correcto, y el enganche no se ejecutaba porque Claude Code no cargaba el archivo — la comprobación habría dicho `BARRERA EN PIE`. Añadida una cuarta condición: el guardián deja un testigo con la hora cada vez que corre y el comprobador exige que sea reciente. Provocado con el testigo envejecido a una hora |
| ⚪ | Sin asignar | **`prueba_tuberia` retirada de la base de pruebas** al ejecutar el vaciado del ensayo remoto. **En producción sigue pendiente**, y le toca a la iteración 24 junto con la creación del esquema. Venía anotada desde la iteración 12. **Aviso comprobado el 2026-09-05: la migración 001 no la borra** —el `.sql` no la menciona—, así que aplicar el esquema en producción **no** salda esta deuda: hace falta un `DROP` escrito a mano, con la comprobación de uuid que exige H-015. **El aviso completo está pegado al primer paso de la iteración 24**, que es donde se va a leer; esta fila sólo lo apunta |
| 🔵 | 21 | **H-015** · `wrangler.toml` **no delimita** contra qué base D1 se escribe en remoto: lo hace el nombre que se teclea. Verificado en el código de wrangler — si el nombre no está en el bloque de entorno cargado, `getDatabaseByNameOrBinding` lo busca en la cuenta con `GET /accounts/<id>/d1/database/<nombre>` y lo encuentra igual. **Consecuencia: declarar una base bajo `env.preview` no la aísla, no declarar una base no la pone fuera de alcance, y un error de tecleo no rebota contra la configuración.** Lo que salvó al proyecto el día de `production` en vez de `produccion` fue que esa base no existiera, no que estuviera bien configurado. Propuesta: que los envoltorios sólo acepten nombres declarados en `wrangler.toml`. **Punto 1 implementado y aprobado:** `verificar-banco` rechaza cualquier `--base` que no esté en `wrangler.toml`, con `PERMITIR_BASE_NO_DECLARADA=1` como única salida, ejercitada de verdad en el ensayo remoto del 2026-09-05. **Puntos 2 y 3 pendientes**, y el 2 —el respaldo pegado al comando, en el mismo bloque— vuelve a importar en cuanto la iteración 24 toque producción |
| 🟢 | 21 | **El veredicto 3 no se provoca omitiendo `--env preview`.** Se documentó así y el ensayo remoto lo desmintió: contra la nube corre igual y devuelve `BANCO CON PROBLEMAS`. La prueba que lo respaldaba se hizo en local, donde sí falla porque allá no hay cuenta a la que preguntar. Error de método: probar en local y concluir sobre remoto. Corregido en el manual con las dos formas que sí lo provocan —un nombre inexistente en la cuenta, y una base real vaciada—, y la corrección queda escrita, no borrada |
| 🟢 | 21 | **`verificar-banco` enviaba su consulta por el extremo de escritura.** Salió de reconstruir H-014: `wrangler d1 execute --remote --file=` viaja por `/import`, no por `/query`, y wrangler avisa de que la base puede quedar sin servir consultas mientras dura. El envoltorio usaba `--file`, así que cada verificación del autor contra la nube habría hecho pasar una consulta de sólo lectura por la puerta de escritura. Ahora lee el `.sql` y lo manda como `--command`. Los tres veredictos reprovocados tras el cambio, sin regresión. Regla anotada en el manual: para leer, `--command`; `--file` sólo cuando se va a escribir |
| 🟢 | Sin asignar | **Las nueve pruebas de restricciones del paso 4 no eran reproducibles. La evidencia sí existe: falta poder repetirla.** Las nueve fueron rechazadas de verdad contra la base de pruebas **en la nube**, cada una con su mensaje, y eso cumple el criterio. Lo que no hay es ningún archivo en `d1/` que permita volver a ejecutarlas: hoy habría que escribirlas otra vez a mano. No cabe en un solo `.sql` porque la primera que falla aborta el resto; necesita un guion que las lance una por una y compruebe que **todas** fallaron. Es deuda de reproducibilidad, no un criterio pendiente . **Saldada el 2026-09-08 en la iteración 23:** `scripts/probar-restricciones.mjs` las lanza una por una, comprueba que las nueve fallen, y deja la base como la encontró —comprobado por resumen criptográfico del contenido, `antes c955a2e298a09426 · despues c955a2e298a09426`, no prometido—. Provocado también el caso contrario: saboteada una prueba para que la base la aceptara, el guion respondió `RESTRICCION CAIDA · 1 de 9`, código 1, y limpió igual. Entra en `npm run verificar` con la semántica del guardián del escapado: sin esquema local dice «no se pudo probar» con código 2, que no es un aprobado |
| 🟢 | Auditoría | **`scripts/verificar.mjs` confundía «git falló» con «el CSS está desfasado», igual que H-013 pero más leve. Corregido el 2026-09-04 a petición del autor:** sólo `status === 1` es `DESFASADO`; cualquier otro código, y un fallo al lanzar el proceso, es `VERIFICACION PARCIAL` mostrando lo que dijo git. Provocado con el caso puro —índice corrupto y ningún desfase real—, que antes daba `DESFASADO` y ahora da `VERIFICACION PARCIAL` con `fatal: .git/index: index file smaller than expected`. Los otros dos veredictos, comprobados sin regresión. Texto original de la anotación: **`scripts/verificar.mjs` confunde «git falló» con «el CSS está desfasado», igual que H-013 pero más leve.** La línea 237 hace `if (diff.status !== 0) → DESFASADO`, y `git diff --exit-code` sólo usa 0 y 1 para «sin diferencias» y «con diferencias»: cualquier otro código es git fallando. Sólo se distingue el caso «no es un repositorio», y por texto del `stderr`. Comprobado con un repositorio de índice corrupto: sale por **128** con `fatal: .git/index: index file smaller than expected`, que no coincide con ese texto y se anunciaría como `DESFASADO`. El arreglo es tratar `status > 1` como `VERIFICACION PARCIAL`, que ese archivo ya tiene. No se hizo en la iteración 21 por quedar fuera de su alcance. **El defecto de paso de argumentos no lo tiene**: revisado en la misma pasada |
| ⚪ | Sin asignar | **Un envoltorio que traduce la salida de otra herramienta necesita un tercer veredicto: «no entendí la respuesta».** Regla que deja H-013. Con sólo dos estados —bien y mal— los fallos propios del envoltorio terminan contados como hallazgos, siempre del lado de «mal», que es el que parece prudente y es el que enseña a ignorar la herramienta. Aplica a `verificar`, a `verificar-banco` y a cualquiera que venga |
| ⚪ | Sin asignar | **Cualquier reemplazo futuro de `verificar` se prueba desde un clon limpio en Windows antes de darse por bueno.** Extendido por H-013 a **todo script que envuelva a otra herramienta**, y con un requisito añadido: provocar de verdad los tres veredictos, incluido el de fallo, no sólo el bueno. La versión vieja dependía del terminal —git en el PATH, H-011— y la nueva dependía de la codificación del disco —CRLF del checkout, H-012—. Son dos fallos de naturaleza distinta y el clon limpio en Windows es el único escenario donde ambos aparecen: en la carpeta de trabajo del autor los dos pasan inadvertidos |
| ⚪ | Sin asignar | **Las marcas de criterios no se editan a mano sin revisar qué más arrastra el cambio.** El criterio 7 de la iteración 12 se marcó aplazado, y una reversión posterior lo devolvió a `[x]` sin que nadie lo notara: el archivo decía una cosa y su bitácora la contraria. Antes de tocar una marca hay que mirar qué otros archivos la citan —bitácora, registro, ADR— y dejarlos de acuerdo |
| 🟢 | — | `.gitattributes` que fije los finales de línea a LF. Hecho al resolver H-012, y con más alcance del previsto: `* text=auto eol=lf`, no solo los CSS. Texto original de la anotación: `.gitattributes` que fije los finales de línea de `static/css/*.css` a LF. Con `core.autocrlf=true`, tras un checkout el archivo queda con CRLF y el generador lo reescribe con LF: `git status` marca los CSS como modificados aunque el contenido sea idéntico. Es ruido cosmético —`git diff` sale vacío y `npm run verificar` no da falsa alarma— pero enseña a ignorar avisos de git, que es justo lo que no conviene |
| ⚪ | Iteración 11 | La tabla «Documentos previstos» de `90-manual/README.md` cita iteraciones 41, 42 y 43 para asuntos de Cloudflare. Son números de la planificación anterior, de cuando Cloudflare era la épica de cierre; con ADR-007 pasó a ser la épica 10. Corregirlos |
| 🟢 | 22 | **H-017** · El guardián del escapado dejaba el contenido hostil dentro de la base local. `process.exit()` no ejecuta los `finally`, y la salida «no se pudo probar» era un `process.exit(2)` **dentro del `try`** que cargaba el veneno. Descubierto al mirar la base antes de preparar la pasada de navegador del autor: la pregunta 900 y el ícono envenenado del módulo 2 seguían dentro desde la corrida anterior, y esa pasada habría hecho dudar del sitio, que estaba bien. Resuelto por tres lados —sondear el servidor **antes** de ensuciar, lanzar en vez de salir, y comprobar la limpieza con una consulta, con un cuarto veredicto `BASE SUCIA`—. Base comprobada limpia después: 10 preguntas, sin la 900, ícono `devices` |
| 🟢 | 22 | **El guardián del escapado corre dentro de `npm run verificar`, y un aviso no se cuenta como un fallo.** El `&&` de antes no servía: cortaba en el primer código distinto de cero y dejaba como veredicto el del último paso. Ahora `scripts/verificar-todo.mjs` lanza los tres, traduce sus códigos con una tabla y da uno solo: `FALLO` → 1, `AVISO` → 2 `VERIFICACION INCOMPLETA`, y un código que la tabla no reconozca se trata como fallo diciendo que era inesperado. Es la regla del tercer veredicto de H-013 aplicada a `verificar`. La barrera sigue siendo la única que corta; las otras dos corren siempre, así que un CSS desfasado ya no deja el escapado sin comprobar. **Tres estados provocados**: con servidor y sano (`OK`), con servidor y `esc()` retirado a propósito (`FALLO`), y sin servidor (`AVISO`, con el comando que lo cierra en la misma línea). El veredicto global `VERIFICADO · 0` es el único que falta por ver, y sólo porque exige el CSS commiteado |
| 🟢 | 22 | **El corolario del `<script>` escrito en el manual**, en `90-manual/escapado-del-banco.md`: la prueba intuitiva —meter un `<script>` y ver si salta una alerta— **diría que todo está bien aunque alguien hubiera quitado el escapado**, porque `innerHTML` no ejecuta `<script>` nunca. Va junto a la prueba que sí sirve, los cuatro veredictos y qué hacer con cada uno. Y con la advertencia del ícono: `icon()` no es presentación |
| 🟢 | 22 | **La protección del ícono del módulo pasó de accidental a deliberada.** Es el único dato del banco que se dibuja dentro de un atributo, lo cubría `icon()` por una decisión de otra iteración y por otro motivo, y ahora el guardián lo prueba explícitamente. Anotado en H-003 y en el manual para que nadie toque `icon()` creyendo que sólo afecta a cómo se ve algo |
| ⚪ | Sin asignar | **`cuestionario.html` pide el banco completo dos veces.** `renderCuestionario()` llama a `leerPreguntas()` y el indicador del pie, `renderEstadoDatos()`, vuelve a llamarlo sólo para contar cuántas trajo. No es un error y no se ve —las dos respuestas son iguales—, pero con 8 preguntas de juguete cuesta poco y con las 368 del banco real cuesta el doble de todo. Detectado el 2026-09-05 al listar lo que la página pide, para preparar la pasada de navegador. Fuera del alcance de la iteración 22: el arreglo natural es que el pie lea del banco ya cargado, y eso toca el orden en que se dibujan las dos cosas |
| 🟢 | 22 | **El generador de la instantánea (`npm run datos:instantanea`), con su sello.** Escribe dentro del archivo contra qué base corrió y cuándo, y de ahí sale literalmente la fecha que el aviso le muestra al estudiante. Las consultas y la validación **se importan** de `functions/api/`, no se copian: con copias propias el respaldo empezaría a traer otra cosa que el extremo el día que una cambiara, y sólo se vería el día de la caída. Tres veredictos provocados: generada; **banco sin preguntas** → `NO SE GENERO` con el archivo anterior intacto, comprobado por hash; y **pisaría una instantánea de la nube** → se niega, que es el fallo silencioso que ADR-023 describe. Heredadas las dos guardas de la 21: base declarada en `wrangler.toml` (H-015) y el modo remoto sólo con `PERMITIR_REMOTO=1` |
| 🟢 | 22 | **El respaldo en el sitio y el aviso visible (ADR-008).** `leerPreguntas()` carga la instantánea con un `import()` dinámico cuando el servicio falla, y devuelve la misma forma de siempre con `meta.respaldo` añadido. El aviso va **arriba del banco**, no en el pie: un aviso discreto se lee después de estudiar, o no se lee. Provocado cuatro veces —sitio sin capa de datos, puerto muerto, capa de datos viva (sin falso positivo) y **banco conectado y vacío**—. La cuarta importa tanto como las otras: un banco vacío **no** activa el respaldo, así que el día que se vacíe por accidente el error no queda escondido detrás de la copia |
| 🟢 | 22 | **H-018** · El respaldo no se activaba si el que contestaba devolvía un error en JSON ajeno. `npm run serve:dist` responde su 404 en JSON cuando se le pide JSON, y el cliente leía `usar_respaldo` de ese sobre: `Boolean(undefined)` es `false`, así que concluía «no es un fallo del servicio» y dejaba al estudiante sin banco y sin respaldo. **El defecto venía de la iteración 12 y ninguna lectura lo había visto**: lo encontró el criterio que exige provocar la caída. Arreglado reconociendo el sobre propio por sus tres marcas antes de leerlo por dentro |
| 🟢 | 22 | **`build-dist.mjs` sigue también los `import()` dinámicos.** El comprobador de enlaces sólo miraba los estáticos, y la instantánea se carga sólo así: era el único recurso del sitio cuya ruta no se comprobaba, y justo el que corre el día que la capa de datos cae. Recursos enlazados: de 16 a 17 |
| ⚪ | 22 | **La instantánea versionada hoy sale de la base LOCAL, o sea del banco de juguete.** Su sello lo dice (`"entorno": "local"`) y `npm run build` lo avisa mientras siga así. **Le toca al autor regenerarla desde la nube antes de publicar** (ADR-023, y ADR-015 no me deja llegar ahí). El comando está en la cabecera de `scripts/generar-instantanea.mjs`; el procedimiento completo, junto con la exportación de `d1/respaldo-banco.sql`, es de la iteración 23. **Añadido el 2026-09-08, para que esta fila no se lea sola:** el sitio se publica **sin** regenerarla desde la nube, y eso **no es un paso olvidado**. Es una decisión expresa y fechada de Felipe Cuevas, tomada porque producción está vacía hasta la iteración 24 y no hay nube desde la cual generar; está escrita como **incumplimiento consciente de ADR-023** en `20-epica-persistencia-preguntas/iteracion-24-migracion-y-ampliacion.md`, pegada al paso «Generar la instantánea desde producción», que es el que lo subsana. El «antes de publicar» de más arriba sigue siendo la regla; lo que cambia es que se sabe que hoy no se cumple y se sabe cuándo se cumplirá. **La instantánea que se publica es la del 2026-09-08, ya limpia** —8 preguntas, ids 1-8, sin la pregunta intrusa de la herramienta de Antigravity—, no la del 2026-09-06 que traía 9 |
| 🟢 | 22 | **H-019** · El generador tapaba el error de wrangler con un fallo de sintaxis propio. Con `--json` wrangler contesta de dos formas por la misma salida —el arreglo de resultados, o un objeto de error— y el generador conocía sólo la primera: arrancaba a interpretar desde el primer `[`, que en el error remoto cae dentro de `notes`. Resultado: un error de sintaxis en la posición 99 en vez de `no such table`, que era el diagnóstico completo. **La hipótesis inicial —un documento JSON por consulta— se comprobó y era falsa**: wrangler imprime uno solo (`cli.js:223739`), local y remoto. Arreglado interpretando la salida entera y mostrando el texto y las notas del error antes que nada. **La lección no es «lo remoto no se puede probar en local»**: era reproducible en local desde el primer día, y no se probó porque los tres veredictos provocados eran situaciones inventadas por mí y no el fallo que la herramienta puede entregar |
| 🟢 | 22 | **Dos guardas nuevas en el generador, para poder probarlo contra la nube sin contaminar el repositorio.** `--ensayo` lee, valida, informa y no escribe nada; y el generador **sólo escribe desde la base del enlace principal de `wrangler.toml`**, la que el sitio publica. Una copia del banco de pruebas versionada como respaldo, con el sello diciendo «nube», desactivaría de golpe todos los demás avisos. Provocado: `NO SE GENERO *** ESA BASE NO ES LA QUE PUBLICA EL SITIO ***`, sin llegar a consultar |
| ⏸️ | 24 | **Criterio aplazado: «existe la instantánea versionada y su contenido coincide con lo que hay en D1».** Decisión del autor, 2026-09-05. **No se aplaza porque la prueba falle: se aplaza porque todavía no hay nada que copiar.** ADR-023 manda generarla desde la nube y la base de producción está vacía, porque aplicar el esquema allá es un paso de la iteración 24. Aunque el JSON se interpretara bien, la consulta cae con `no such table: modulo` — y eso fue literalmente lo que dijo. Anotado en la iteración 24 como dependencia explícita pegada al paso del esquema y la carga: esquema → carga → generar la instantánea desde producción → commit. **No se genera desde pruebas como sustituto**, por decisión expresa del autor |
| 🟢 | 24 | **H-020** · **El aviso `MODULE_TYPELESS_PACKAGE_JSON` de Node en los guiones que importan módulos del sitio.** `package.json` no declara `"type": "module"`, así que Node reinterpreta cada `.js` de `static/js/` al importarlo y lo avisa. No afecta al navegador ni a Cloudflare: sólo a los guiones que corren en Node, y cuesta una segunda pasada de análisis por archivo. **Actualizado el 2026-09-08 (iteración 23): ya son tres los caminos por donde asoma** —el generador de la instantánea, el guardián del escapado y ahora `scripts/administrar-banco.mjs`, que importa `functions/api/_validacion.js`—, y cada guion nuevo que toque código del sitio suma uno. Eso cambia la urgencia sin cambiar la gravedad: el aviso sale encima de la salida de tres guardianes cuyo valor depende de que su salida se lea, y H-012 ya dejó escrito que un aviso presente en cada corrida enseña a ignorar los avisos. **Se resuelve en la iteración 24**, por decisión del autor. Arreglo: añadir `"type": "module"` y renombrar `tailwind.config.js` a `.cjs`, que es el único archivo CommonJS del proyecto (comprobado). Es pequeño pero toca la construcción, así que no se hace dentro de una iteración cuyo alcance es otro. **Cerrado el 2026-09-09.** `"type": "module"` declarado en `package.json` y `tailwind.config.js` renombrado a `tailwind.config.cjs`, que era el único archivo CommonJS de verdad del proyecto —los otros dos que aparecían al buscar `module.exports` lo tenían **dentro del texto de una pregunta**—. Los tres caminos devuelven **cero** ocurrencias del aviso. Y lo que había que comprobar era que no rompiera nada: **el CSS compilado quedó byte a byte idéntico** (`0bb489dca2eedfb544533a3d3dff04aa`), que es la prueba de que Tailwind encontró su configuración `.cjs`; si no la hubiera encontrado habría compilado con la paleta por omisión y el archivo sería otro. `functions/` compila, las dos páginas y los doce módulos ES se sirven en 200. **Lo que no se puede comprobar desde aquí es la construcción de Cloudflare Pages**: eso sólo lo prueba el despliegue |
| 🟢 | 24 | **Publicar: `git push` a `main` está retenido hasta que la iteración 24 cargue el banco.** Única deuda operativa que deja la iteración 22. Empujar **es** publicar —cada push a `main` dispara la construcción y publica solo, `90-manual/publicacion-en-cloudflare-pages.md`— y desde la 22 el cuestionario se sirve de D1. Con producción sin esquema ni banco, el sitio publicado quedaría en modo degradado sirviendo la instantánea, que hoy trae las ocho preguntas de juguete: nada roto, nadie enterado. Anotada como **última tarea de la iteración 24**, con la lista de lo que tiene que estar antes de empujar. **La lista está escrita** desde el 2026-09-09, en la iteración 24, sección «Lista de comprobación del push», y tiene dos mitades: cinco puntos antes de empujar y siete después de publicar. La segunda mitad existe porque esta entrega toca el sistema de construcción —`"type": "module"` y `tailwind.config.cjs`— y saca un número escrito a mano del HTML, y **las dos cosas fallan de una forma que la verificación local no puede ver**: la paleta por omisión de Tailwind y la insignia diciendo «105» sólo se ven en el sitio publicado. **Publicado el 2026-09-09** en el commit `950b313`, y las siete comprobaciones de después salieron en verde sobre el sitio: paleta correcta —la prueba de que la construcción de Pages encontró `tailwind.config.cjs`—, insignia ausente, mensaje de banco conectado y vacío, sin aviso de respaldo, y los dos extremos coincidiendo en `preguntas_activas: 0` / `banco_vacio: true` / `meta.vacio: true`, con `/api/estado` **ya sin `meta.vacio`**. Cerrada |
| 🟢 | 22 | **El manual de publicación describía los veredictos viejos de `npm run verificar`.** Quedó desactualizado por un cambio de esta misma iteración —el coordinador de tres comprobaciones— y justo en la sección que se lee **antes de cada push**. Corregido con los tres veredictos nuevos y con la causa más habitual de `VERIFICACION INCOMPLETA`, que es no tener levantado `npm run datos:dev`. Detectado al ir a comprobar si un push publica solo: la documentación de lo que uno cambia envejece en el mismo acto de cambiarlo |
| 🟢 | 22 | **H-019, comprobado contra la nube por el autor.** `--ensayo` contra `examen-td-js-pruebas`, en PowerShell y en Git Bash con resultado idéntico: `ENSAYO CORRECTO`, 8 preguntas, 32 alternativas, ninguna descartada, y el archivo versionado sin tocar. Con eso el arreglo queda comprobado **por el camino remoto real**, que es el único donde el defecto se manifestaba, y de paso queda ejercitado el modo que permite probar contra la nube sin contaminar el repositorio |
| ⚪ | 23 | **La 23 va antes que la 24, y eso deja dos de sus criterios dependiendo de la 24.** La herramienta de administración se construye y se prueba contra diez filas de juguete —mismo riesgo que ADR-024 asumió para la 22— y el procedimiento en un solo bloque que encarga ADR-023 —editar, exportar, regenerar, publicar— **no se puede recorrer de punta a punta**, porque su último tramo toca producción y producción no tiene ni esquema ni banco hasta la 24. El bloque se escribe en la 23; se camina en la 24. Anotado en el archivo de la 23 **antes de empezarla**, con la tabla de qué se cierra allá y qué queda dependiendo, para que no aparezca al final como un olvido |

---

## Épicas sin implementar

Ideas para más adelante. No están planificadas y no tienen carpeta. Al promoverse a
épica se les asigna número y se les crea carpeta desde Claude web.

- **Fichas de repaso.** Convertir las preguntas del banco en tarjetas de memoria con
  repetición espaciada.
- **Diagnóstico inicial.** Un cuestionario corto que estime el nivel del estudiante
  y le recomiende por qué módulos empezar.
- **Historial de intentos.** Guardar los simulacros rendidos y mostrar la evolución
  del puntaje en el tiempo.
- **Glosario interactivo.** Términos del temario explicados, enlazados desde las
  justificaciones de las preguntas.
- **Modo docente.** Vista que agregue estadísticas de las preguntas más falladas,
  para orientar las clases. Con D1 esto pasa a ser viable de verdad, pero requiere
  resolver cómo recolectar datos sin romper el principio de cero fricción ni
  identificar a ningún estudiante.
- **Contenido por módulo.** Apuntes propios de cada módulo, no solo el listado de
  temas evaluados.
- **Instalable como aplicación.** Que el sitio se pueda instalar y funcione sin
  conexión.
